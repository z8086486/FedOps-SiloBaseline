# User-editable FedOps Federated Task configuration.
random_seed: 42
learning_rate: 0.001

# FedOps 1.2-compatible transport type. This Baseline currently supports PyTorch.
model_type: Pytorch
model:
  # Add only the constructor values consumed by model.build_model(config).
  name: replace-with-model-name

dataset:
  name: replace-with-dataset-name
  # Agent Studio overrides this with a local data binding for owner/participant runs.
  root: ./dataset
  validation_split: 0.2
  download: false

# FedOps Web/Agent Studio inject the actual runtime key when a run starts.
task_id: task_id

wandb:
  use: false
  key: ""
  account: ""
  project: ${dataset.name}_${task_id}

num_epochs: 1
batch_size: 32
num_rounds: 2
clients_per_round: 1

server:
  strategy:
    _target_: flwr.server.strategy.FedAvg
    fraction_fit: 0.00001
    fraction_evaluate: 0.000001
    min_fit_clients: ${clients_per_round}
    min_available_clients: ${clients_per_round}
    min_evaluate_clients: ${clients_per_round}

runtime:
  manager_port: 8004
