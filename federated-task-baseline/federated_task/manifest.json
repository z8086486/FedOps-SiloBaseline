{
  "schemaVersion": 1,
  "name": "replace-with-task-tool-name",
  "description": "Describe how an Initial or Global Model is used as an Agent Builder Tool.",
  "entrypoint": "federated_task.tool:predict",
  "modelFormat": "safetensors",
  "input": {
    "type": "object",
    "required": ["replace_with_input"],
    "properties": {
      "replace_with_input": {
        "description": "Replace this placeholder with the Tool input contract"
      }
    }
  },
  "output": {
    "type": "object",
    "required": ["replace_with_output"],
    "properties": {
      "replace_with_output": {
        "description": "Replace this placeholder with the Tool output contract"
      }
    }
  }
}
